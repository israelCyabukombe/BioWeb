name: Ping backend

on:
  schedule:
    - cron: '*/5 * * * *'
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest

    steps:
      - name: Ping health endpoint
        run: curl -fsS https://bioweb-server-fkf6c7b0d2dkdede.centralus-01.azurewebsites.net/api/health